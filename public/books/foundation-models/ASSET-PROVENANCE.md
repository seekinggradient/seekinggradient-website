# Foundation-model book asset provenance

The four cover images in `covers/` were extracted unchanged from the validated EPUB editions in `downloads/`. They were produced by the deterministic AgentsFS EPUB publication template and are not AI-generated raster illustrations.

The website interface is implemented in HTML and CSS. The two AI-generated conceptual illustrations embedded in the online book content retain faithful normalized generation prompts in `src/books/GENERATED-ASSET-PROMPTS.md`.
